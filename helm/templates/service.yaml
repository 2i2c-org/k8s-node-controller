apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-node-controller.fullname" . }}
  labels: {{- include "k8s-node-controller.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      {{- with .Values.service.nodePort }}
      nodePort: {{ . }}
      {{- end }}
  selector: {{- include "k8s-node-controller.selectorLabels" . | nindent 4 }}
